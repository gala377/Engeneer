poprawki

get_next_token_with_blank

json.org lexowanie floata

tabela ascii etx zamiast '\0'

tablica ascii na funkcje zamiast mapy

//
do zapytania

type modifiers (const, &(as ptr), [] array?)
parsing struct body with type modifiers
errors for lexer
postfixes
prefixes


//
DO ZROBIENIA

zbieranie informacji w trakcie parsowania

lexowanie komentarzy
lexowanie floatów
lexowanie escapowania w stringach ?


skończyc parsowanie expression <- dodanie operacji modulo
refacotorowac parsowanie ciala struktury
typ tablicowy
wyznaczyc typy podstawowe (uint8-64, int8-64, float64-256, chaaaar??? )
wraps powinno pozwalac na wiecej niz jedna strukture, obecnie jest tylko jedna

    if else
    while
    ? for <- serio?
    return
    throw, catch? <- eeeeee?
    break; continue <- to chwila
    code blocki itd